<%=render 'layouts/admin'%>

<div class="col-xs-12 col-sm-6 col-md-8">
	<br>
	<h2>Ver categoría</h2> 
	<p>
		<strong>Nombre:</strong>
		<%= @category.name%>
	</p>
	<p>
		<%= link_to 'Atrás', categories_path %> | <%= link_to 'Editar', edit_category_path(@category) %>
	</p>
</div>

