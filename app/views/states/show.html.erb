<%=render 'layouts/admin'%>

<div class="col-md-9">
	<div class="col-sm-offset-2 col-sm-10">
		<h2>Ver estado</h2> 
		<p>
			<strong>Nombre:</strong>
			<%= @state.name%>
		</p>
		<p>
			<%= link_to 'Atrás', states_path %> | <%= link_to 'Editar', edit_state_path(@state) %>
		</p>
	</div>
</div>
